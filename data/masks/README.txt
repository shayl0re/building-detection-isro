Place your segmentation masks (.PNG) here.

Masks are grayscale binary images where:
  - Pixel value = 0   → Background (roads, vegetation, water, etc.)
  - Pixel value > 0   → Building footprint

Each mask must correspond 1-to-1 with an image in data/images/,
with matching filenames (different extension: .PNG instead of .JPG).
